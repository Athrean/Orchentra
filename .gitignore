node_modules/
dist/
.next/
*.db
*.db-journal
*.db-wal
*.db-shm
orchentra.yml
.env
.env.*
!.env.example
.DS_Store

# Credentials — NEVER commit
secrets/
*.pem
*.key
*.p12
*.pfx

# Turbo cache
.turbo/

# TS incremental build artifacts
*.tsbuildinfo

# IDE
.vscode/
.idea/

# Product docs. All of docs/ is local-only — nothing under it is tracked
# (canonical.md, roadmap.md, proposals, etc. stay off the public repo).
docs/
# Tracked product docs that live alongside the app they document
!apps/*/docs/
UBIQUITOUS_LANGUAGE.md

# Local agent/IDE plugin folders (not part of the product repo)
.claude-plugins/
.claude/plugins/

# Claude Code local settings (may contain secrets)
.claude/settings.json
.claude/skills/
.claude/hooks/
.claude/worktrees/
.claude.env
.mcp.json

# Local sprint planning notes (not part of product repo)
.claude/*.md
/handover.md

# Local session state
.orchentra/

# Local agent state / scratch dirs — not part of this repo
.claude/
.entire/
.agents/
skills-lock.json
.charlie/
.commandcode/

# Heavy media kept locally for a future animated hero bg — not committed
back-frames
*.mp4
