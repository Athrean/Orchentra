// Phase 2 brain skeleton — episodes, runbooks, and the SKILL.md export format
// that turns distilled runbooks into context an external agent can load.
//
// This file re-exports the public surface as each slice lands. Today it is
// intentionally empty; the smoke test under tests/ verifies the module
// resolves so downstream packages can depend on it before any concrete
// types are added.
export {}
